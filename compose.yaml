services:
  fashions-store:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    image: fashions-store:latest
    container_name: fashions-store
    ports:
      - "3000:80"
    restart: unless-stopped
