Đọc cấu trúc thư mục Pages/Auth và sửa những cấu trúc thư mục trong các folder khác theo cấu trúc thư mục của Pages/Auth
Dùng headerLayout thay cho navbar để dùng chung headerLayout. 
Sửa lại giao diện màn hình chính cho các khối hòa hợp với nhau, tất cả màn hình cùng lấy 1 font chữ, banner để Admin tùy chỉnh thêm Thêm vào voucher các field như: màu nền, ảnh banner, thứ tự ưu tiên (sort_order), tiêu đề phụ. Admin điền khi tạo voucher, frontend đọc và render theo.

